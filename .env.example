# Optional server settings. Plaid credentials are configured in Moneta's
# Settings page and stored in the local SQLite database.
# PORT=4173
# HOST=127.0.0.1
# FINANCE_DB_PATH=/absolute/path/to/finance.db
